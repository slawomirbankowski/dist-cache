package com.cache.interfaces;

/** interface for Spaces manager.
 * Spaces are set of Space objects to handle distributed and  shared objects with versioning.
 * */
public interface Spaces extends DistService {

}
